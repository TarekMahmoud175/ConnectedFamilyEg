import { ScrollDashboardDirective } from './scroll-dashboard.directive';

describe('ScrollDashboardDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollDashboardDirective();
    expect(directive).toBeTruthy();
  });
});
